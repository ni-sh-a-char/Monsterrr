# Render deployment configuration for Monsterrr
services:
  - type: web
    name: monsterrr-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn monsterrr.main:app --host 0.0.0.0 --port 8000"
